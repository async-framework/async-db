/* eslint-disable */
// This file is generated by db. Do not edit by hand.

export type PageStatus = "draft" | "review" | "published";

export type PageBlocksItemKind = "hero" | "markdown";

/** CMS pages rendered by a tiny schema-driven UI example. */
export type Page = {
  /** Stable page id. */
  id: string;
  /** Page title shown in the CMS list. */
  title: string;
  /** Publication state. */
  status?: PageStatus;
  /** Page author. */
  authorId: string;
  /** Short page summary. */
  summary?: string;
  /** Markdown body content. */
  bodyMarkdown?: string;
  /** Nested page blocks rendered by the recursive example UI. */
  blocks?: Array<{
    /** Block renderer key owned by the app. */
    kind: PageBlocksItemKind;
    /** Block heading. */
    title?: string;
    /** Block body markdown. */
    bodyMarkdown?: string;
    settings?: {
      /** Theme token for this block. */
      color?: string;
      /** Whether this block is highlighted. */
      featured?: boolean;
    };
  }>;
};

/** Authors that can own CMS pages. */
export type User = {
  /** Stable author id. */
  id: string;
  /** Author display name. */
  name: string;
  /** Author email address. */
  email: string;
};

export type DbCollections = {
  pages: Page;
  users: User;
};

export type DbDocuments = {
};

export type DbTypes = {
  collections: DbCollections;
  documents: DbDocuments;
};

export type DbCollectionName = keyof DbCollections;
export type DbDocumentName = keyof DbDocuments;
